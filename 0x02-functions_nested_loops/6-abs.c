#include "main.h"

/**
 * int _abs - Write a function that computes the absolute value of an integer
 *
 * return: Aways 0.
 */

int _abs(int c);
{
	if (c > 0 || c == 0)
	{
		return (c);
	}
	else
	return (c * -1);
}

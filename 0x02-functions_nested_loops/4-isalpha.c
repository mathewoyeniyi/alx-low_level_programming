#include "main.h"

/**
 * main - check for alphabet charater
 *
 * @c: alphabet to pass to 4-isalpha.c
 *
 * Return: Always 0
 */

int _isalpha(int c)
{
	if ((c >= 'a' && c <= 'z') || (c >= 'A' && c = 'Z'))
	{
		
		return (1);
		}
		else
		return (0);
	}
